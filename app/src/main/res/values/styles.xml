<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!-- Switch styles -->
    <style name="FilterSwitchThemeRed" parent="AppTheme">
        <item name="colorAccent">@color/mmi_red</item>
        <!-- this line won't work because we're using this on a SwitchMaterial, not a plain Switch: <item name="android:colorControlActivated">@color/mmi_red</item> -->
    </style>
    <style name="FilterSwitchThemeYellow" parent="AppTheme">
        <item name="colorAccent">@color/mmi_yellow</item>
    </style>
    <style name="FilterSwitchThemeblue" parent="AppTheme">
        <item name="colorAccent">@color/mmi_blue</item>
    </style>
    <style name="FilterSwitchThemeViolet" parent="AppTheme">
        <item name="colorAccent">@color/mmi_violet</item>
    </style>
    <style name="FilterSwitchThemeGreen" parent="AppTheme">
        <item name="colorAccent">@color/mmi_green</item>
    </style>
    <!-- app themed -->
    <style name="FilterSwitchBlueAppTheme" parent="AppTheme">
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

</resources>
