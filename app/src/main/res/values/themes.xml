<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The theme applied to the application or activity -->
    <style name="CustomActionBarTheme"
           parent="@android:style/Theme.Holo.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabStyle">@style/ActionBarTabStyle</item>
        <item name="android:actionBarTabBarStyle">@style/TabBarStyle</item>
    </style>

    <!-- ActionBar styles -->
    <!-- This will change the background color of the action bar -->
    <style name="MyActionBar"
           parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/dark_blue</item>
    </style>
    
    <!-- This changes the appearance of the selected underline bar on the navigation tabs -->
    <style name="ActionBarTabStyle" 
        parent="@android:style/Widget.Holo.ActionBar.TabView">
    	<item name="android:background">@drawable/actionbar_tab_indicator</item>
    </style>
    
    <!-- Action bar tab bar -->
    <!-- This will change the background color of the navigation tabs -->
    <style name="TabBarStyle"
           parent="@style/Widget.AppCompat.ActionBar.TabBar">
    	<item name="android:background">@color/darker_blue</item>
    </style>
    
    <!-- Progress bar -->
    <style name="ProgressBarTheme" parent="@style/_ProgressBarTheme"/>

	<style name="_ProgressBarTheme" parent="Theme.AppCompat.Light.DarkActionBar">

    <item name="android:progressBarStyleHorizontal">@style/ProgressBarProgressBarTheme</item>

	</style>
    
</resources>